# Project fixture: invalid ds.toml

This negative fixture verifies that malformed project configuration remains a
structured project diagnostic; loading falls back to the default `main.del`
entry only so diagnostics remain total.
