# Project fixture: ds.toml entry point

This fixture is a minimal DEL project with a project-owned `ds.toml` entry
point. The source files are authored for this repository and exercise only
project discovery and import provenance.

| File | Kind | Expected outcome |
|---|---|---|
| `ds.toml` | project configuration | valid; `entry_point = "src/main.del"` |
| `src/main.del` | DEL source (configured entry) | `ok`; imports `lib.del` |
| `src/lib.del` | DEL source (imported) | `ok` |
